class Goal {
    constructor(activity_level) {
        this.activity_level = activity_level
    }
    /** 
    static calculateCalGoals(activity_level) {
        if (activity_level === "sedentary") {
            return Math.round(Calorie.total_calories * 1.2)
        }
        else if (activity_level === "lightly active") {
            return (Calorie.total_calories * 1.375)
        }
        else if (activity_level === "moderately active") {
            return Math.round(Calorie.total_calories * 1.375)
        }
    } **/ 
}